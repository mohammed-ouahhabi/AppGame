{# templates/developpeur/show.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Détails du développeur{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Détails du développeur</h1>
        <table class="table table-bordered">
            <tr>
                <th>ID</th>
                <td>{{ developpeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ developpeur.nom }}</td>
            </tr>
        </table>
        <div class="text-center">
            <a href="{{ path('developpeur_index') }}" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
        </div>
    </div>
{% endblock %}
