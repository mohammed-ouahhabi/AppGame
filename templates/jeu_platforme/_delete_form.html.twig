<form method="post" action="{{ path('app_jeu_platforme_delete', {'id': jeu_platforme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeu_platforme.id) }}">
    <button class="btn">Delete</button>
</form>
