{% extends 'base.html.twig' %}

{% block title %}JeuPlatforme index{% endblock %}

{% block body %}
    <h1>JeuPlatforme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Plateforme</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jeu_platforme in jeu_platformes %}
            <tr>
                <td>{{ jeu_platforme.id }}</td>
                <td>{{ jeu_platforme.plateforme }}</td>
                <td>
                    <a href="{{ path('app_jeu_platforme_show', {'id': jeu_platforme.id}) }}">show</a>
                    <a href="{{ path('app_jeu_platforme_edit', {'id': jeu_platforme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_jeu_platforme_new') }}">Create new</a>
{% endblock %}
